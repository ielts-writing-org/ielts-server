{
  "name": "ielts-server",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/standard-validator": "^0.4.0",
    "@inferdi/hono": "^6.0.1",
    "@inferdi/inferdi": "^6.0.1",
    "hono": "^4.13.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
